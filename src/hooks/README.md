# hooks

Shared React hooks not owned by a single feature (e.g. a future
`useSession` hook once Milestone 2 introduces Owner/Driver sessions).
Empty for now — feature-specific hooks live inside their own
`features/<name>/` folder instead.
